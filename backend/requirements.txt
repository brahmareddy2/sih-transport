# ═══════════════════════════════════════════════════════════
# AI-Powered Logistics DSS — Backend Dependencies
# Phase 1: Foundation only
# ═══════════════════════════════════════════════════════════

# ── Web Framework ─────────────────────────────────────────
fastapi==0.111.0          # Async web framework with auto OpenAPI docs
uvicorn[standard]==0.29.0 # ASGI server (includes websockets, watchfiles for reload)

# ── Database ──────────────────────────────────────────────
sqlalchemy==2.0.30        # ORM with async support
alembic==1.13.1           # Database migrations
psycopg2-binary==2.9.9    # PostgreSQL adapter (binary = no system deps)

# ── Validation & Settings ─────────────────────────────────
pydantic==2.7.1           # Data validation (FastAPI uses this internally)
pydantic-settings==2.3.0  # Load config from .env files into typed Settings object

# ── Authentication & Security ─────────────────────────────
passlib[bcrypt]==1.7.4    # Password hashing with bcrypt
python-jose[cryptography]==3.3.0  # JWT creation and verification
python-multipart==0.0.9   # Required for FastAPI form data (login forms)

# ── Async Task Queue ──────────────────────────────────────
celery==5.4.0             # Distributed task queue for async optimization jobs
redis==5.0.4              # Python Redis client (Celery broker + result backend)

# ── HTTP Client ───────────────────────────────────────────
httpx==0.27.0             # Async HTTP client for calling external APIs (ORS, weather)

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.1      # Load .env files (supplemental to pydantic-settings)

# ── Phase 2 additions (commented out until needed) ─────────
# ortools==9.10.4067       # Google OR-Tools for VRP optimization
# prophet==1.1.5           # Meta Prophet for demand forecasting
# xgboost==2.0.3           # XGBoost for delay risk prediction
# scikit-learn==1.4.2      # sklearn utilities
# numpy==1.26.4            # Numerical computing
# pandas==2.2.2            # Data manipulation for ML/analytics
# Faker==24.11.0           # Synthetic data generation (seed_data.py)
